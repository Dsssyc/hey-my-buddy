interface:
  display_name: "DeepSeek Delegate"
  short_description: "Hand bounded coding tasks to local dsh runs"
  default_prompt: "Use $deepseek-delegate to hand this bounded task to a local dsh run, then inspect the actual diff and run the relevant checks yourself."
policy:
  allow_implicit_invocation: true
